apiVersion: v1
kind: Service
metadata:
  labels:
    name: cassandra
  name: cassandra
spec:
  ports:
    - port: 9042
      name: cql
  resources:
      limits:
        memory: "400Mi"
      requests:
        memory: "200Mi"
  selector:
    name: cassandra

